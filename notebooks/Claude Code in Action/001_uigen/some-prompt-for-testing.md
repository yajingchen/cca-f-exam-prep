# Example 1 - custom commands:

/audit


# Example 2 - custom commands with arguments :

/write_tests the use-auth.ts file in the hooks dir


# Example 3 - Playwright MCP:

Your goal is to improve the compmonent generation prompt at @src/lib/prompts/generation.tsx . Here's how:

1. Open a browser and navigate to localhost: 3000
2. Request a basic component to be generated
3. Review the generated component and its source code
4. Identify areas for improvement
5. Update the prompt to produce better components going forward.

For now, only evaluate visual styling aspects. We don't want components generated that look like typical tailwindcss components - we want something more original. 


# Example 4 – GitHub Integration, Mention Action (Issue):

Title: Toggle Buttons

Description: 

Sometimes it feels like the toggle buttons aren't working. 

Here are the ones I'm talking about:

<img width="338" alt="Image" src="https://github.com/user-attachments/assets/xxxxx" />

@claude can you take a look at this? Just test them out and verify that clicking them toggles between the code and preview tabs.